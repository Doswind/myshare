# 主力基金持仓分析系统功能说明书

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 系统名称 | 主力基金持仓分析 |
| 文档版本 | 1.0 |
| 编写依据 | 当前代码实现（包括当前工作区未提交改动） |
| 编写日期 | 2026-08-15 |
| 前端技术 | React 18、TypeScript、Vite、React Query、Zustand |
| 后端技术 | Python、FastAPI、SQLAlchemy、APScheduler |
| 数据库 | SQLite（WAL 模式） |
| 外部数据源 | 东方财富、腾讯行情接口 |

> 本说明书描述当前代码行为。项目中部分 README 内容已落后于实际实现，例如用户系统、RBAC、概念板块和当前默认筛选阈值，应以本说明书及代码为准。

## 2. 产品定位

系统用于从公开基金数据中筛选符合条件的基金，抓取基金季报重仓股票，并以股票为核心提供：

- 基金持仓汇总；
- 股票所属行业和概念板块；
- 最新行情；
- 持仓基金数量及持仓规模；
- 行业板块比较；
- 自选股管理；
- 基金、股票详情查询；
- 抓取任务、任务日志和审计日志管理。

系统的核心分析链路为：

```text
抓取基金列表
    ↓
按抓取策略筛选基金
    ↓
抓取筛选基金的详情和季报持仓
    ↓
持仓股票按股票代码去重
    ↓
抓取股票名称、行业、概念板块
    ↓
抓取当前行情
    ↓
按行业/其它板块展示
```

## 3. 用户和权限

### 3.1 未登录用户

未登录用户只能访问：

- 登录；
- 忘记密码；
- 重置密码。

### 3.2 普通登录用户

普通用户可以根据分配的权限访问：

- 持仓看板；
- 基金列表和基金详情；
- 股票列表和股票详情；
- 自选股；
- 个人资料；
- 具有权限时访问设置和任务功能。

### 3.3 管理员

管理员在普通用户能力基础上，可以使用：

- 用户管理；
- 角色和权限管理；
- 审计日志；
- 任务清理；
- 抓取策略和任务配置。

权限控制由前端路由和后端接口共同完成，不能仅依赖前端隐藏按钮。

## 4. 页面功能说明

### 4.1 登录页 `/login`

功能：

- 用户名/密码登录；
- 登录成功后保存访问令牌；
- 登录失败提示错误；
- 未登录访问业务页面时跳转登录页。

### 4.2 忘记密码 `/forgot-password`

功能：

- 输入用户名或邮箱；
- 发起密码重置申请；
- 展示申请结果。

### 4.3 重置密码 `/reset-password`

功能：

- 根据重置令牌设置新密码；
- 校验令牌有效性；
- 设置成功后返回登录。

### 4.4 持仓看板 `/`

持仓看板是系统首页，展示当前基金快照对应的持仓股票。

#### 筛选条件

- 最低基金规模；
- 最低近一年收益；
- 股票价格区间；
- 行业；
- 股票市场板块；
- 是否仅显示有基金持仓的股票；
- 排序方式：
  - 持仓基金数；
  - 涨跌幅；
  - 持仓市值。

#### 板块展示

- 有行业信息的股票按行业分组；
- 没有行业信息的股票统一归入“其它”；
- 每个板块卡片默认展示前 6 只股票；
- 点击“查看全部”查看板块内完整股票；
- 股票卡片显示：
  - 股票名称；
  - 股票代码；
  - 最新价格；
  - 涨跌幅；
  - 被多少只基金持仓；
  - 持仓市值。

#### 数据口径

看板只读取当前保存基金最新报告期的持仓股票，不直接展示历史残留在 `stock` 表中的股票。

### 4.5 基金列表 `/funds`

功能：

- 分页查看基金列表；
- 按代码、名称、类型搜索；
- 查看基金规模、净值、近一年收益；
- 查看基金经理、基金公司等详情摘要；
- 点击进入基金详情。

### 4.6 基金详情 `/funds/:code`

展示：

- 基金代码和名称；
- 基金类型；
- 单位净值和累计净值；
- 规模；
- 近一年收益；
- 风险等级；
- 基金评级；
- 基金经理；
- 基金公司；
- 成立日期；
- 最新报告期持仓；
- 重仓股票及持仓比例。

### 4.7 股票列表 `/stocks`

提供两种主要查看方式：

1. 行业/板块视图；
2. 自选股视图。

功能：

- 股票名称和代码搜索；
- 行业筛选；
- 市场板块筛选；
- 价格区间筛选；
- 按基金数量、涨跌幅、持仓市值排序；
- 分页查看股票；
- 查看股票详情；
- 加入或移除自选股。

股票列表的“共 N 只”表示当前筛选条件下的后端总数；前端不使用固定的 500、1000 或 2000 条上限。未指定分页时，板块接口返回当前条件下完整结果。

### 4.8 股票详情 `/stocks/:code`

展示：

- 股票名称、代码；
- 市场和证券 ID；
- 最新价格；
- 涨跌幅；
- 成交量和成交额；
- 市值；
- PE、PB 等行情指标；
- 所属行业；
- 所属概念板块；
- 持仓该股票的基金；
- 持仓基金数量；
- 持仓总市值。

### 4.9 设置 `/settings`

设置页包含：

#### 基金筛选阈值

- 基金最低规模；
- 近一年最低收益；
- 价格范围等默认筛选项。

当前基金抓取使用保存的规模和年收益配置。例如：

```json
{
  "min_scale": 5,
  "min_ret_1y": -10
}
```

#### 抓取任务

可查看和触发：

- 基金+持仓；
- 仅刷新持仓；
- 行情；
- 行业+成分股；
- 股票详情；
- 基金详情。

页面展示：

- 任务运行状态；
- 任务日志；
- 任务开始和结束时间；
- 成功/失败状态；
- 错误信息；
- 冷却倒计时。

### 4.10 个人资料 `/profile`

功能：

- 查看当前账号信息；
- 修改个人资料；
- 修改密码；
- 退出登录。

### 4.11 用户管理 `/admin/users`

管理员功能：

- 用户列表；
- 创建用户；
- 修改用户；
- 启用/禁用用户；
- 分配角色；
- 重置密码；
- 查看用户状态。

### 4.12 角色管理 `/admin/roles`

管理员功能：

- 创建角色；
- 修改角色；
- 分配权限；
- 查看角色关联用户；
- 删除或停用角色。

### 4.13 审计日志 `/admin/audit`

记录和查询：

- 操作用户；
- 操作类型；
- 操作目标；
- 操作详情；
- 请求 IP；
- 操作时间；
- 操作成功或失败状态。

## 5. 抓取功能说明

### 5.1 基金+持仓完整流水线

完整任务执行以下步骤。

#### 第一步：抓取基金列表

从外部基金数据源抓取基金基础列表和业绩指标。

唯一业务筛选条件为抓取策略配置的：

- `scale_yi >= min_scale`
- `ret_1y >= min_ret_1y`

不再使用历史主力标记、旧持仓列表或股票数量作为基金筛选条件。

#### 第二步：抓基金详情和持仓

以第一步筛选后的基金代码为唯一来源：

- 抓取基金详情；
- 抓取最新季报重仓股票；
- 没有股票持仓的基金不保存到当前基金列表；
- 同一股票被多个基金持仓时，在 `stock` 表中只保存一份；
- 基金与股票之间的关系保存到 `fund_holding`。

数据关系示例：

```text
fund
  基金 A
  基金 B

stock
  600036 招商银行

fund_holding
  基金 A - 600036
  基金 B - 600036
```

#### 第三步：抓持仓股票详情

以第二步产生的去重股票代码为范围，抓取：

- 股票名称；
- 行业代码；
- 行业名称；
- 概念板块；
- 市场和证券标识。

行业信息缺失时，股票仍然保留，并在看板中归入“其它”。

#### 第四步：抓最新行情

仍然只针对第二步的去重股票代码抓取：

- 最新价；
- 涨跌幅；
- 成交量；
- 成交额；
- 总市值；
- PE、PB 等行情字段。

东方财富批量接口未返回的股票会尝试腾讯接口补抓，并记录成功数量和缺失数量。

### 5.2 数据快照替换

基金+持仓任务在远程抓取全部完成且没有失败后，才替换旧数据：

1. 清理旧 `fund_holding`；
2. 清理旧 `fund`；
3. 写入有有效股票持仓的新基金；
4. 写入新的持仓关系；
5. 同步股票基础信息。

如果基金列表或持仓抓取中途失败，保留旧数据，避免出现半套新数据。

### 5.3 仅刷新持仓

以当前保存的基金列表为基础：

- 重新抓取当前报告期持仓；
- 清理没有股票持仓的基金；
- 替换当前持仓快照；
- 对当前去重股票刷新股票详情、行业、板块和行情。

### 5.4 行情刷新

默认范围为当前保存基金最新报告期持仓股票，不读取历史 `stock` 表中的全部股票。

行情接口未返回的数据不会伪造，页面显示 `--`，同时任务结果会报告缺失数量。

### 5.5 行业和板块刷新

抓取：

- 行业板块；
- 概念板块；
- 板块成员股票。

在当前持仓模式下，只维护当前持仓股票的板块关系，不将全市场板块成员混入持仓看板。

### 5.6 基金详情刷新

基金详情刷新以当前 `fund` 表基金代码为基础，更新：

- 风险等级；
- 基金评级；
- 基金经理；
- 基金公司；
- 成立日期；
- 规模等字段。

## 6. 冷却、防重复和任务恢复

### 6.1 运行中防重复

同一任务在运行期间再次触发会被拒绝，避免重复抓取。

典型返回：

- HTTP 409：任务正在运行；
- HTTP 429：任务仍处于完成后冷却期。

### 6.2 冷却时间

| 手动任务 | 冷却时间 |
|---|---:|
| 基金+持仓 | 30 分钟 |
| 仅刷新持仓 | 30 分钟 |
| 行业+成分股 | 15 分钟 |
| 行情 | 1 分钟 |
| 股票详情 | 30 秒 |
| 基金详情 | 10 分钟 |

### 6.3 手工任务和定时任务互斥

同一逻辑任务组的手工任务和定时任务不能同时运行，例如：

- 手工基金+持仓与基金全量定时任务互斥；
- 手工行情与行情定时任务互斥；
- 手工行业刷新与行业定时任务互斥。

### 6.4 卡住任务处理

数据库中的 `running` 日志超过 30 分钟时，可被识别为异常卡住任务并清理。

管理员可以手动清理卡住任务。清理会：

- 将日志标记为失败；
- 写入清理原因；
- 释放进程内任务锁。

## 7. 定时任务

系统启动时初始化数据库、RBAC 数据和 APScheduler 调度器。

当前任务类型包括：

- 行情；
- 基金净值/基金详情；
- 行业板块；
- 股票详情；
- 基金全量及持仓。

定时任务的启用状态、执行频率、执行时间、交易日限制等由抓取配置控制。

支持的调度模式包括：

- 关闭；
- 每日；
- 间隔执行；
- 每周定时；
- 交易日限制；
- 时间窗口限制。

修改任务配置后，系统重新注册对应调度任务。

## 8. 数据模型

### 8.1 Fund 基金表

主要字段：

- `code`：基金代码，主键；
- `name`：基金名称；
- `fund_type`：基金类型；
- `nav`：单位净值；
- `scale_yi`：规模；
- `ret_1y`：近一年收益；
- `risk_level`：风险等级；
- `rating`：评级；
- `manager`：基金经理；
- `company`：基金公司；
- `establish_date`：成立日期。

### 8.2 FundHolding 基金持仓表

主要字段：

- `fund_code`：基金代码；
- `report_date`：报告期；
- `stock_code`：股票代码；
- `stock_name`：持仓时返回的股票名称；
- `shares`：持股数量；
- `market_value`：持仓市值；
- `ratio_nav`：占基金净值比例；
- `ratio_net`：占股票流通股比例。

一个基金可持有多只股票，一只股票也可被多个基金持有。

### 8.3 Stock 股票表

主要字段：

- `code`：股票代码，主键；
- `name`：股票名称；
- `market`：市场标识；
- `secid`：行情接口证券标识；
- `industry_code`：行业代码；
- `industry_name`：行业名称；
- `concept_codes`：概念板块代码；
- `details_fetched_at`：详情更新时间。

### 8.4 StockQuote 行情表

保存股票多次行情快照，主要字段：

- `code`：股票代码；
- `price`：最新价；
- `change_pct`：涨跌幅；
- `volume`：成交量；
- `amount`：成交额；
- `market_cap`：总市值；
- `pe`：市盈率；
- `pb`：市净率；
- `snapshot_at`：快照时间。

看板读取每只股票最新一条行情。

### 8.5 Sector 行业/板块表

主要字段：

- `code`：板块代码；
- `name`：板块名称；
- `kind`：industry/concept/region；
- `change_pct`：板块涨跌幅；
- `lead_stock`：领涨股。

### 8.6 SectorMember 板块成员表

维护板块和股票之间的多对多关系：

```text
sector.code ↔ stock.code
```

### 8.7 Watchlist 自选股表

保存用户自选股票：

- 用户；
- 股票代码；
- 备注；
- 创建时间。

### 8.8 JobLog 任务日志表

记录：

- 任务 ID；
- 任务名称；
- 状态；
- 开始时间；
- 结束时间；
- 错误信息；
- 关联用户。

### 8.9 RBAC 数据

包括：

- 用户；
- 角色；
- 权限；
- 用户角色关系；
- 角色权限关系；
- 审计日志。

## 9. 后端接口模块

接口统一前缀为 `/api`，详细参数可通过 `/docs` 或 `/redoc` 查看。

### 9.1 基金 `/api/funds`

提供：

- 基金列表；
- 基金分页；
- 基金搜索；
- 基金详情；
- 基金持仓；
- 基金统计。

### 9.2 持仓 `/api/holdings`

核心接口：

- 按行业查询持仓股票；
- 按筛选条件聚合；
- 过滤价格区间；
- 过滤市场板块；
- 按基金数量、涨跌幅、持仓市值排序。

未指定分页参数时返回完整结果。

### 9.3 股票 `/api/stocks`

提供：

- 股票列表；
- 股票详情；
- 股票最新行情；
- 股票持仓基金；
- 股票搜索；
- 自选股关联操作。

### 9.4 板块 `/api/sectors`

提供：

- 行业板块列表；
- 概念板块列表；
- 行业名称列表；
- 板块成分股；
- 板块行情。

行业缺失兜底项为“其它”。

### 9.5 筛选 `/api/filters`

提供：

- 获取默认筛选条件；
- 保存规模、收益、价格和行业筛选条件。

### 9.6 任务 `/api/jobs`

提供：

- 手动触发抓取任务；
- 查询任务日志；
- 查询运行中任务；
- 查询冷却状态；
- 清理卡住任务；
- 查看任务执行结果。

### 9.7 自选 `/api/watchlist`

提供：

- 查询用户自选股；
- 添加自选股；
- 删除自选股；
- 修改备注；
- 查询自选股行情。

### 9.8 抓取配置 `/api/crawl-config`

提供：

- 查询任务配置；
- 修改任务启用状态；
- 修改调度类型；
- 修改执行时间；
- 修改间隔；
- 修改交易日和时间窗口；
- 重新加载任务。

### 9.9 RBAC 接口

提供：

- 当前用户信息；
- 用户管理；
- 角色管理；
- 权限查询；
- 审计日志查询。

## 10. 数据一致性规则

### 10.1 基金来源规则

当前 `fund` 表只保存：

- 满足抓取策略筛选条件；
- 有有效股票持仓；
- 属于当前快照的数据。

### 10.2 持仓来源规则

看板使用当前保存基金最新报告期的 `fund_holding`，不使用旧报告期或已删除基金的孤立持仓。

### 10.3 股票去重规则

股票以 `stock.code` 去重，多个基金持有同一股票时只保留一条股票基础记录。

### 10.4 行业兜底规则

行业名为空、空字符串或无法回填时，统一显示为“其它”，不得从看板隐藏。

### 10.5 行情缺失规则

行情接口未返回时：

- 不伪造价格或市值；
- 前端显示 `--`；
- 任务结果记录缺失数量；
- 可通过后续行情任务重试。

## 11. 异常处理

### 11.1 外部接口失败

- 单批请求失败时尝试备用数据源；
- 单只股票或基金失败会记录日志；
- 完整基金快照任务在关键抓取失败时保留旧数据；
- 行情任务报告成功和缺失数量。

### 11.2 数据库写入失败

- 基金和持仓快照在事务中写入；
- 写入异常执行回滚；
- 避免旧数据被清空后无法恢复。

### 11.3 任务重复触发

- 运行中返回 409；
- 冷却期间返回 429；
- 前端显示运行状态和冷却倒计时。

### 11.4 服务重启

进程内锁会在重启时丢失，数据库中的运行日志提供恢复兜底。超过卡住阈值的任务可由管理员清理。

## 12. 部署和启动

### 12.1 后端

工作目录：

```text
backend
```

推荐使用项目虚拟环境：

```bash
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
```

开发模式：

```bash
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

健康检查：

```text
GET /api/health
```

接口文档：

```text
/docs
/redoc
```

### 12.2 前端

工作目录：

```text
frontend
```

安装依赖并启动：

```bash
npm install
npm run dev
```

生产构建：

```bash
npm run build
```

默认开发端口：

```text
5173
```

## 13. 当前已知限制

1. `JobGuard` 主要是进程内锁，多进程或多实例部署时不能提供严格的分布式互斥，建议生产环境使用 Redis 或数据库锁。
2. 外部行情接口可能对北交所或特殊代码返回空数据，这类股票会显示 `--`。
3. 行业和概念数据依赖外部接口，接口限流或返回不完整时，股票会进入“其它”。
4. `stock` 表仍可能保留基础历史记录，但看板只使用当前持仓集合。
5. 当前完整抓取会访问大量外部接口，执行时间受网络速度、接口限流和基金数量影响。
6. SQLite 适合单机或低并发场景，多实例部署需要迁移到支持并发写入的数据库。
7. CORS 当前允许所有来源，正式生产环境应限制为明确前端域名。
8. 默认配置、README 和部分历史文案可能与当前代码不完全一致，应以设置页和实际接口行为为准。
9. 外部数据源返回的股票名称、行业和行情并不保证始终完整，系统提供持仓名称兜底和行情缺失提示，但不能凭空生成不存在的行情数据。

## 14. 核心文件索引

| 功能 | 主要文件 |
|---|---|
| 应用入口和路由注册 | `backend/app/main.py` |
| 基金接口 | `backend/app/api/funds.py` |
| 持仓接口 | `backend/app/api/holdings.py` |
| 股票接口 | `backend/app/api/stocks.py` |
| 板块接口 | `backend/app/api/sectors.py` |
| 任务接口 | `backend/app/api/jobs.py` |
| 抓取配置接口 | `backend/app/api/crawl_config.py` |
| 基金抓取服务 | `backend/app/services/fund_service.py` |
| 持仓聚合服务 | `backend/app/services/aggregation.py` |
| 股票服务 | `backend/app/services/stock_service.py` |
| 任务锁和冷却 | `backend/app/utils/job_guard.py` |
| 定时任务 | `backend/app/scheduler/scheduler.py` |
| 基金模型 | `backend/app/models/fund.py` |
| 持仓模型 | `backend/app/models/holding.py` |
| 股票模型 | `backend/app/models/stock.py` |
| 行情模型 | `backend/app/models/quote.py` |
| 选股 OpenClaw 客户端 | `backend/app/services/openclaw_client.py` |
| 选股 Mock 数据 | `backend/app/services/screener_mock.py` |
| 选股接口 | `backend/app/api/screener.py` |
| OpenClaw 代理接口 | `backend/app/api/openclaw.py` |
| 前端路由 | `frontend/src/App.tsx` |
| 持仓看板 | `frontend/src/pages/DashboardPage.tsx` |
| 股票列表 | `frontend/src/pages/StockListPage.tsx` |
| 选股分析 | `frontend/src/pages/ScreenerPage.tsx` |
| 选股候选表 / 因子 / AI 对话 | `frontend/src/components/screener/{CandidateTable,FactorPanel,AIChatDialog}.tsx` |
| 选股 SSE / Prompt 工具 | `frontend/src/lib/{sseStream,promptBuilder}.ts` |
| 设置和任务页面 | `frontend/src/pages/SettingsPage.tsx` |
| 用户管理 | `frontend/src/pages/admin/UsersAdminPage.tsx` |
| 角色管理 | `frontend/src/pages/admin/RolesAdminPage.tsx` |
| 审计日志 | `frontend/src/pages/admin/AuditLogPage.tsx` |

## 15. 选股分析（Screener Lab）

### 15.1 定位

将持仓看板的"被动观察"升级为"主动筛选 + AI 尽调"：从候选池挑出值得人工细看的股票，并对每只候选提供客观量化画像 + AI 深度分析。决策仍由人做。

相对通用选股软件的**唯一独占优势**是：拥有主力基金持仓明细，因此打分体系必须把"主力资金行为"作为一等公民。

### 15.2 页面入口

`/screener`，与"持仓看板 / 基金列表 / 股票列表"在同一导航栏。复用 `dashboard:view` 权限（暂未单独设 `screener:view`）。

页面自上而下三块：
1. **顶部筛选条**：行业下拉 + 最低分数输入
2. **候选表卡片**：表格 + 行展开因子面板 + 行操作"智能析股"按钮
3. **通用 AI 对话卡片**：页面级内嵌，自由提问不绑定股票

### 15.3 量化候选池（demo）

当前为 **22 只 mock 候选**，覆盖 11 个行业，故意覆盖 4 种典型形态：

| 形态 | 含义 |
|---|---|
| 低位高分（候选） | 位置因子低位 + 主力资金积极加仓 → 推荐重点关注 |
| 高位低分（陷阱） | 位置因子高位 + 主力资金减仓 → 警示风险 |
| 低位低分（观望） | 位置低但无主力动作 → 暂不关注 |
| 中位高分（追强） | 中位 + 趋势向上 + 主力继续重仓 → 跟随强者 |

数据形态仅用于演示，**不反映真实行情**。

### 15.4 因子与评分体系

每只候选按五组因子量化：

| 组 | 字段 |
|---|---|
| **位置因子** | 近 1 年价格百分位、近 3 年价格百分位、距高点回撤、相对 MA60 的 z-score、RSI(14)、布林带位置、连续下跌天数、20 日均量比 |
| **趋势与动量** | MA20/60/250 方向、近 20 / 60 日收益率 |
| **主力资金行为** | 持有该股的主力基金数、合计持仓市值、季度环比加减仓、持仓基金平均近 1 年收益、"聪明钱"质量分 |
| **估值** | PE-TTM 及近 3 年百分位、PB |
| **重仓基金明细** | 基金代码 / 名称 / 规模 / 近 1 年收益 / 占净值 / 市值（5–10 只） |

四维评分：**位置 / 趋势 / 主力资金 / 估值**，每维 0–100 分，总分为加权求和（权重后期可配）。

### 15.5 智能析股（单股，右抽屉）

- **触发**：候选行右侧"智能析股"按钮（lucide Sparkles 图标）
- **打开**：右抽屉滑出，先展示 promptBuilder 预填的结构化提示词（含该股因子 + 持仓明细），可一键切到编辑模式修改
- **发送**：后端 `/api/openclaw/chat` SSE 透传 OpenClaw `/v1/responses`
- **流式渲染**：AI 回复逐 chunk 追加到消息气泡，`react-markdown` 渲染 GFM（`skipHtml` 防注入）
- **多轮对话**：`previous_response_id` 由 sessionStorage 维护；关闭抽屉不丢失，可继续追问
- **sessionStorage 隔离**：按 `openclaw:chat:{stockCode}` 隔离，每个股票独立会话
- **限流**：同股 60s 内重复点击发送按钮 disable
- **中断恢复**：SSE 中断时保留已渲染内容 + 黄色横幅 + 用上次的 `previous_response_id` 重连
- **关闭时取消**：AbortController 终止进行中的 fetch

### 15.6 通用 AI 对话（页面内嵌卡片）

- **位置**：候选表卡片下方独立卡片，候选表同级
- **不绑定股票**：`code` 字段不传给后端 → 后端 `_build_enriched_message` 透传不注入
- **sessionStorage 隔离**：`openclaw:chat:general` 与单股对话完全独立
- **限流**：同入口 60s 全局
- **首屏**：空状态文案"开始对话吧 — 提问后按 Ctrl/Cmd + Enter 发送"

### 15.7 OpenClaw 后端代理

- **官方接口**：`POST {base_url}/v1/responses`（OpenResponses API，OpenAI 协议兼容）
- **认证**：`Authorization: Bearer <token>`（token 存 `.env`，已 gitignore，前端永不接触）
- **流式**：`stream: true` → SSE
- **远程访问**：`ssh -N -L 18789:127.0.0.1:18789 user@网关机器`
- **异常分类**：`OpenClawAuthError`（401）/ `OpenClawTimeoutError` / `OpenClawUnavailableError`（网络/5xx/未配置 token）/ 基类 `OpenClawError`
- **HTTP 异常映射**：AuthError → 401 + 提示检查 token；TimeoutError → 504；UnavailableError → 503（token 缺失时也返回 503 + 明确文案）

#### 路由清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/openclaw/health` | 探测 `{base_url}/health`，返回 `{ok, reachable, version, has_token}`，用于前端顶部横幅 |
| POST | `/api/openclaw/chat` | 流式：`code` 可选（缺时透传不注入），SSE 响应 |
| POST | `/api/openclaw/chat/sync` | 同步版：demo 期仅打日志（耗时 + 响应长度 + response_id） |
| POST | `/api/openclaw/feedback` | demo 期仅打日志 |

### 15.8 选股接口清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/screener/candidates?industry=&min_score=` | 候选列表，按 total_score 倒序，返回精简字段 |
| GET | `/api/screener/factors/{code}` | 单只因子详情（不含 holders），404 if not found |
| GET | `/api/screener/holders/{code}` | 单只重仓基金列表，404 if not found |
| GET | `/api/screener/industries` | 候选行业列表（用于筛选下拉） |

### 15.9 前端组件分层

```
ScreenerPage
├── 顶部 OpenClaw 健康横幅（每 30s 探测）
├── 候选表卡片
│   ├── CandidateTable（含「智能析股」按钮 + 分数着色）
│   └── FactorPanel（5 组因子 + 重仓基金 mini 表，行下折叠）
├── 单股 AI 析股抽屉（AIChatDialog variant="drawer" mode={stock}）
└── 通用 AI 对话卡片
    └── AIChatDialog variant="embedded" mode="general"
```

**AIChatDialog 双形态**：
- `variant="drawer"`：固定右侧抽屉（带遮罩，需 `open` + `onClose`）
- `variant="embedded"`：内联卡片（无遮罩，消息区 `maxHeight: 480px`）

**AIChatDialog 双模式**：
- `{ kind: "stock", stock }`：预填结构化 prompt，按 stock.code 隔离
- `{ kind: "general" }`：无预填，自由输入，sessionStorage 用 `openclaw:chat:general`

### 15.10 当前边界

**本期已做**：mock 候选池、量化因子画像、单股 AI 尽调、通用 AI 对话、OpenClaw HTTP 代理、SSE 流式、多轮对话、sessionStorage 持久化、限流、中断恢复。

**本期明确不做**（待后续迭代）：
- 真实抓取（东财 K 线 / AKShare 接入）
- 因子计算引擎（百分位 / z-score / 均值回归统计全部 mock）
- DB schema 改动（不新建表 `stock_kline_daily` / `stock_factor`）
- RBAC 新权限码（`screener:view`，复用 `dashboard:view`）
- 独立智能问答中心（per-stock chat drawer 是本期唯一对话入口）
- 策略模板 / 权重可配置
- 候选池跟踪 + 胜率统计
- 图片 / 文件上传到 OpenClaw（API 支持，本期不接）
- 客户端 tools 注入（让 AI 调用本系统 API）

### 15.11 安全

- `OPENCLAW_TOKEN` 仅通过 `.env` 注入；`.env` 已 gitignore；`.env.example` 仅含占位字段
- 文档中如出现真实 token 立即脱敏（曾发生，已修）
- AI 输出始终 `react-markdown skipHtml` 防 HTML/脚本注入
- 前端 SSE 请求手动携带 Bearer token（不走 axios interceptor，因为不走 `client.ts`），token 来源 `localStorage.getItem("access_token")`

